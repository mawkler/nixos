# NOTE: Causes issues with `nix develop` when that flake uses a non-nightly version
# [target.x86_64-unknown-linux-gnu]
# rustflags = [
#     "-Z", "share-generics=y", # Share generic code over multiple compiler units
#     "-Z", "threads=8", # Expand macros in parallel
# ]

[build]
jobs = 20 # number of parallel jobs
